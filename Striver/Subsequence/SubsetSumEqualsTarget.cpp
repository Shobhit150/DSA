#include <iostream>
using namespace std;

bool bruteForce(vector<int> &arr, int target, int i) {
    if(i==arr.size()) {
        return false;
    }
}
int main() {
    vector<int> arr = {1, 2, 7, 3};
    int target = 6;
}
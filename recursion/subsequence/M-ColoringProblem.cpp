#include <iostream>
#include <vector>
using namespace std;

class Solution {
  public:
    bool recur(int v, vector<vector<int>> &edges, int i, int m, vector<int> &color) {
        if(i==v) return true;

        

    }
    bool graphColoring(int v, vector<vector<int>> &edges, int m) {
        vector<int> color(v,-1);

    }
};

void solve() {
    
}

int main() {

    int t = 1;
    // cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}